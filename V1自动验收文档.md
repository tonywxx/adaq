# ADAQ V1 自动验收文档

验收日期：2026-09-01（含 2026-08-31 历史基线）
仓库：`https://github.com/tonywxx/adaq.git`
审计基线：`d085d2677a191e8e2d6744ed0b24d4df67c572b6`
验收范围：Issue `#159` Gate 15、Specification `#197`、Implementation `#198`

## 当前头实测补充（2026-09-01）

本节是重新 build 后的当前头实测记录；下方 2026-08-31 内容保留为历史基线，不覆盖本节事实。

### 当前结论

- 本轮没有修改 App 认证逻辑，没有增加进程级凭据缓存，没有把 OKX 密钥复制到 env 文件或其他磁盘文件。
- 测试只复用了同一个已授权的打包进程；当前 debug App 重启后没有再次出现 Keychain 授权提示。应用退出或切换到新进程后，系统是否提示由 macOS Keychain 自身决定，不能由验收脚本绕过。
- EMA5/EMA10 因子、Candidate Revision 和 Strategy Qualification 已在当前头完成真实证据链；`gate12Eligible=1`、`gate12ContinuationRequired=0`。
- 进入 Paper Trading 后，Host 正式对账仍从 OKX Demo 收到 HTTP 401 / `50113 Invalid Sign`。因此没有保留 Paper Account，没有创建 Bot，也没有生成虚假的订单、成交、持仓或 PnL。
- V1 当前不能标记为全部验收通过；阻塞点是外部 OKX Demo 私有接口对账证据，不是本地用缓存或 env 规避的问题。

### 1. 当前头逐步记录

1. **重新构建与临时授权处理**
   - 构建命令：`pnpm tauri build --debug`，TypeScript/Vite 与 Rust 构建通过。
   - Desktop：`/Users/tony/github/adaq/src-tauri/target/debug/bundle/macos/adaq.app`。
   - Bundle ID：`bid.adaq.desktop`。
   - 只保留一个当前打包进程供 Computer Use 复用；没有启动第二个 AdaQ 实例，也没有写入 `.env`、Keychain 替代文件或日志密钥。

2. **EMA 因子与 Component evidence**
   - 因子：`b0b5b98f-7e79-4c83-b5f7-7e654f3fbec8`，输出为 `buy-signal`。
   - 当前 Promotion Decision：`d356cdaa-e795-4184-b8c2-219c9008bc4b`，13 个 eligibility gates 均通过，Component eligible。
   - 逻辑：第一次 EMA5 上穿 EMA10 记录当时 EMA5；第二次上穿且高于前次记录值才输出买入信号；没有把不存在的 market high 冒充为前高。

3. **Strategy Lab 与 Qualification**
   - Candidate Revision：`e3977b3b-af04-4ab1-8a62-c8981f5c60b9`，revision hash `0fbde482dea71206ee2f2f7ddae444b7aceba00957ad9b1746584062e9e35700`。
   - Qualification Attempt：`6689b5e8-449c-408d-8ef0-ee1bdd214431`，状态 `ready-for-review`。
   - Strategy Qualification：`11529b4837415a230f19fd57c7633d5ff38ef1adf4940dfa8bf6db0b02041a01`，UI 中明确复核 Candidate、Package 和 evidence 后创建成功。
   - 使用 OKX/crypto 冻结上下文、Universe、3 个 signal datasets，并完成 9 个参数网格 portfolio backtest；Qualification 结果为可继续进入 Paper 前置流程。

4. **打包 Desktop 的 Paper Trading 验收**
   - Computer Use 进入中文 `模拟交易` 页面，页面初始不主动联系 Provider，并显示“没有保留的 OKX Demo 账户证据”。
   - 点击“对账”并确认后，正式 Host reconciliation 返回：`HTTP 401: {"msg":"Invalid Sign","code":"50113"}`。
   - UI 保留 fail-closed 错误：“对账失败。保留的证据未改变。OKX Demo 未返回已对账的账户证据。”随后取消弹窗；数据库仍无 Paper Account，Bot 部署入口没有被越权打开。

5. **对账问题的最小排查与回退**
   - Host HTTP boundary 按 OKX Demo 签名规则请求 pending orders，并携带 `instType=SPOT` 与 Demo header；本地 mock test 验证了请求路径和订单解析。
   - 为区分路径问题，曾对同一已授权凭据做过一次 history endpoint 诊断，真实响应同样为 401 / `50113`；正式代码已恢复为 pending-order reconciliation 路径。
   - 没有修改认证策略、没有绕过签名校验、没有用本地假订单或数据库记录伪造账户证据。

### 当前验收状态

| 项目 | 当前结果 |
|---|---|
| Debug Desktop build | 通过；精确包路径和 `bid.adaq.desktop` 已验证 |
| EMA Factor / Component | 通过；当前 Component eligible |
| Strategy Qualification | 通过创建；`gate12Eligible=1`、`gate12ContinuationRequired=0` |
| OKX Demo reconciliation | 阻塞；HTTP 401 / `50113 Invalid Sign` |
| Paper Account | 未创建；无保留的对账账户证据 |
| Bot / orders / fills / PnL | 未运行；没有伪造结果 |
| Issue `#159` | 保持 open；未把 Gate Parent 当实现 issue 关闭 |

后续只有在用户重新验证有效的 OKX Demo 私有接口凭据/环境、且对账成功后，才能继续创建 Paper Account、部署模拟盘 Bot、记录运行结果并完成剩余 Desktop 验收。本轮不需要也不会把密钥临时写入 env。

## 历史基线：2026-08-31 Gate 15 结论

Gate 15 System Dashboard 已实现，并在最新 macOS ARM64 debug Desktop App 中用 Computer Use 完成 en-US 与 zh-CN 的产品路径验收。根路径会按 Host 的 operational responsibility 在 Workflow Guide 与 System Dashboard 之间切换；Dashboard、Operations、研究证据和 Paper/Bot/Feedback drill-down 均保持用户隔离、只读和显式 Unknown/empty/unavailable 状态。

本次验收还真实执行了一个 EMA5/EMA10 因子从 Feature Context、Candidate 构建、Dataset 物化、Evaluation 到 Promotion Decision 的完整链路。链路最终正确停在 `Rejected / Not eligible`，没有强行创建 Component、Strategy、Paper Account 或 Bot。因此“模拟盘 Bot 运行”没有被伪造为成功；当前系统以 fail-closed 方式阻止部署，真实原因和后续条件记录在 [V1模拟盘Bot分析报告.md](V1模拟盘Bot分析报告.md)。

Issue `#159` 仍保持 open，等待用户个人验证和明确 Workflow Module Acceptance / Continuation Approval；没有在本次验收中关闭 Gate Parent。CI 运行 `33380241990` 仍按限制记录为 pending，不称为 green。

## 1. 开始前的实时范围与依赖检查

1. 读取 Git remote，确认唯一远端为 `tonywxx/adaq`，没有根据本地路径猜测仓库所有者。
2. 检查工作区：保留用户已有的未跟踪 ADR `docs/adr/0096-stage-gate-14-locale-acceptance-before-v1-completion.md`，未覆盖无关改动。
3. 查询实时 GitHub 状态：
   - `#158` Gate 14 已关闭，是已接受的前置输出。
   - `#159` open，当前 `blocked_by #158`，并阻塞实现子 issue `#198`。
   - `#197` specification open，`#198` implementation open、带 `ready-for-agent`，其 live 图仍受 `#159` 约束。
4. 按 Gate Parent 规则，未把 `#159` 当作实现目标，也没有关闭它。

## 2. 实现内容

### 2.1 System Dashboard 与根路由

1. Native 新增用户隔离的 `system_dashboard` Host read command：身份只来自当前窗口认证状态，调用方不能传入任意 `user_id`。
2. Projection 组合现有 owning-domain read models：Health、Alerts、最近 Operational Events、Bot Runtime Attempt、Paper Account 原生货币金额/订单/成交/持仓/对账、研究/回测/验证/Paper Feedback 计数。
3. 单个 owning read 失败时只标记相应 section 为 unavailable，并显示空值/Unknown；不会把其他区误报为 Healthy。
4. `/` 在有 Bot、Paper Account、未解决告警或关键 operational read unavailable 时显示 System Dashboard；没有 operational responsibility 时仍显示 Workflow Guide；`/help/workflow` 保持可访问。
5. Dashboard 的链接只进入所属工作区，不执行下单、撤单、Bot 生命周期、对账、告警确认/清除或 V1 readiness 决策。
6. 金额始终按账户原生 `market/currency` 展示，没有跨币种求和；新增 en-US 与 zh-CN 文案，并覆盖 loading、empty、unavailable、stale、restart、Unknown 和权限边界。

### 2.2 两个真实缺陷的根因修复

1. Factor admission 原先会把通用 Component qualification evidence 的 JSON 数组误当作 Factor evidence。现在只接受 Factor-specific object；数组会被排除，损坏 object fail closed，并新增回归测试。
2. Custom Candidate build 原先没有把当前冻结的 Factor Context predecessor 传入保存层，导致 Candidate 虽构建成功，后续 Dataset 找不到匹配 Candidate。现在由 Tauri command 从当前用户 Context 生成 predecessor 并一路传入 queue/job/save，新增回归测试；最终 Candidate 已绑定 Revision 3。
3. Feature Materialization 选择新的 Snapshot 后，表单原先可能保留旧时间范围，导致第一次 Dataset 落在前一天。现在 Snapshot 选择会同步 `startTimeMs/endTimeMs` 到本地 datetime 表单；旧错误 Dataset 保留为历史证据，最终 Dataset 使用正确窗口。

### 2.3 EMA5/EMA10 因子

新增 [factor-ema-crossover](examples/components/factor-ema-crossover/) 示例 Component：

- Feature Slots：`ema-5`、`ema-10`，均来自冻结的 close Feature Dataset。
- 输出：`buy-signal`。
- 第一轮 bullish crossover（EMA5 从不高于 EMA10 变为高于 EMA10）记录当时的 EMA5 值。
- 下一轮 bullish crossover 仅在当前 EMA5 高于上一次记录值时输出 `buy-signal = 1`，其余已交付行输出 `0`。
- 该数据集没有单独的 market `high` slot，所以“前高”明确实现为第一次交叉时的 EMA5 值，而不是冒充 market high。
- Factor 状态跨 process batch 保持；第一条交付行按 warmup 返回无输出。

## 3. 自动检查与构建证据

### 3.1 通过的检查

| 检查 | 结果 |
|---|---|
| `cd src-tauri && cargo fmt --all -- --check` | 通过 |
| `cd src-tauri && cargo check --workspace` | 通过，0 errors，23 个既有 warning |
| `cd src-tauri && cargo test --workspace` | 通过，562 passed，13 ignored，42 suites |
| Factor predecessor 定向 Rust test | 通过，1 passed |
| Factor generic evidence 定向 Rust test | 通过，1 passed |
| `pnpm exec jest --watchman=false --runInBand` 相关 3 suites | 通过，14 passed |
| `pnpm run build` | 通过，严格 TypeScript + Vite |
| `pnpm run lint` | 通过，172 files |
| `git diff --check` | 通过 |
| Component `cargo test --offline --locked` | 通过 |
| Component build / package verify | 通过，WASM Component ABI 与包校验通过 |

完整前端 Jest 历史检查也已通过：47 suites、166 tests。

### 3.2 已知限制

- `pnpm run format:check` 只剩两个既有、未由本次改动触及的 Python Research 文件需要格式化：`src/features/python-research/python-model-lab-panel.test.tsx` 与 `src/features/python-research/python-model-lab-panel.tsx`。
- Vite 报告一个已有的 ineffective dynamic import 提示，以及一个超过 500KB 的 chunk 提示；不影响构建成功。
- Rust 保留既有 23 个 warning；本次没有为了 Gate 15 做无关清理。
- GitHub CI `33380241990` 在记录时仍是 pending；本地和 Desktop 证据不把它升级为 green。

## 4. Debug Desktop App 验收准备

1. `pnpm tauri dev` 只启动过一次，并等待该实例完成初始化。
2. 因为后续需要替换构建产物，使用最新 debug bundle 重启应用：
   - App：`/Users/tony/github/adaq/src-tauri/target/debug/bundle/macos/adaq.app`
   - Desktop target：`bid.adaq.desktop`
3. 每次替换 bundle 后重新获取 App state；遇到旧窗口句柄失效时，按 display name `adaq` 恢复，没有启动第二个 AdaQ 实例。
4. 首次打开显示初始化状态，等待工作区完成后再读取 AX tree；没有把初始化中的空内容误判为失败。

## 5. en-US Computer Use 逐步验收

### 5.1 首页与运行面板

1. 打开 `/`，确认标题为 `System Dashboard`。
2. 确认 Dashboard 显示：
   - `Local system Critical`，来源 `host · freeze_all_requested`。
   - `Execution adapter Healthy`，来源 `okx-demo`。
   - `Paper account Unknown`、`Risk / OMS Unknown`，没有伪造健康状态。
   - unresolved Critical 数量可见。
   - `No Bot Runtime records are retained.`
   - `No OKX Demo Paper Account is retained.`
   - Research evidence 区和 owning workspace links 可见。
3. 进入 `/operations`，确认 Host Health、Alert、Operational Evidence 和 read-only projection 边界可见；Dashboard 没有控制按钮。

### 5.2 Feature Context

1. 进入 `/features`，确认当前 Research Evidence Context 为 Revision `3`。
2. 确认市场/场所为 `crypto · okx`，Snapshot 为 `c4e2fe2e4eaaa5758ff7934cd479716668bb349ac4050cc48c9d050e5ea0eb53`。
3. 确认观察窗口为 `1787881320000 → 1787882400000`。
4. 确认 Feature Dataset 为 `24987d6e7385a83800ddbf79248cf54aeee6d0c427f1ede7aa1d3b836c614310`，Feature Plan 为 `f38b6a57d9e4ff1ee88225ce06a29afd68aaf70fa47810478e498b56b8770afe`，PIT Universe 为 `universe-42a3cb9e36e30c96ee58ca13f2b17ce67551229e65d19d65af848fe057f5cc2e`。

### 5.3 Factor Candidate

1. 进入 `/factors` 的 Candidate tab，确认可用 Feature output 为 `ema-5, ema-10`。
2. 受控构建第一次正确失败：Attempt `f3a4a03c-8684-4271-a088-60e9e7a0c5e7`，诊断为 `built Factor package does not match the frozen Candidate schema`；根因是示例 manifest 的 component ID 与 Candidate ID 不一致。
3. 修复 manifest ID 后，第一次成功 build 发现下游没有 predecessor；这暴露了保存层绑定缺失，而不是被 UI 掩盖。
4. 修复 predecessor 传递后重新构建：
   - Attempt：`52d32782-8000-42ee-83a8-1f8918f070f2`
   - Candidate hash：`ca1c448b5c59cd55ba25ae61798df37bec58a60b3fc98a8d6e8aae913e8df829`
   - Component package SHA-256：`21f208d07cfb0059713281ac20a376d2760fa4059b259f7042b97abc0730f66f`
   - Component source SHA-256：`8ded354492fc526e15e619e441d2b5b1af298cfd06dcc7897487a3e6a5c79480`
   - 状态：`completed 1/1`
5. 在 Candidate 记录中确认绑定 Context Revision 3，而不是仅看到一个孤立的 Candidate hash。

### 5.4 Factor Dataset

1. 切换 Dataset tab，确认自动选中 `EMA 5/10 Crossover Buy Signal · ca1c...`。
2. 启动物化，最终 Attempt：`417daa2a-32fc-43d4-bbf4-c5d499d10791`。
3. 确认进度 `18 / 18`、状态 `completed`，结果 Dataset：`748e01682169a69e6a9df2dc9b65664a0f335c5c134ff08a06f0ab514b1ce9d1`。
4. 记录内容证据：payload SHA-256 `c5d649981565b882b19031bc09d4fcb7cb60f6e1a3b3520ac9579b5484bf24e8`，Parquet SHA-256 `ae9784daa40c6ce50af8038688153d4ae672bd5550ef143da3527774a575f576`。
5. 之前错误窗口产生的 Dataset `1a287e4e10250376419892a2baea22df417d432755cd321728385381b3a7705b` 保留在历史记录中，未被删除或改写；最终记录使用正确窗口。

### 5.5 Factor Evaluation

1. 切换 Evaluation tab，确认 Dataset 为 `748e...`、输出为 `buy-signal`、证据状态为 `out-of-sample`。
2. 启动 Evaluation，确认 Attempt `5835d9cf-3a15-471a-bd6f-332c8655fc7b` 完成 `1/1`。
3. 确认 Report hash：`553113b5d23b26ace299fbe3cc8a740bdbabd5b2685a0a0e9701589bdee389b9`。
4. 关键指标显示为真实不可用状态，而不是补零冒充有效结果：可用样本 9、missingness 1、sample-count 0；IC/rank-IC 为 insufficient samples；economic/turnover/decay 为 no eligible observations；stability 为 insufficient samples。

### 5.6 Promotion Decision 与资格检查

1. 切换 Decision tab，选择 Candidate、Dataset、`buy-signal`、Report 和 Promotion Policy。
2. 冻结 Promotion Protocol：`86a6cc715a5df4afcbaeaaf74b793bc2da316c19d9aae180d1d5643a5a2b4a94`。
3. 记录 Decision：
   - Decision ID：`17c1d3a8-2767-4df6-a54a-e94574a629ff`
   - Decision hash：`7aa2ead700a0243bdc12aee126acd1f7c1be74941a0a1e29185b7c9df7d893c9`
   - Policy hash：`38cc89d52a2a333e903236ccbf562ef2224c5955e350d6516dc0310c60698e0b`
   - 状态：`Rejected`
4. 点击 Eligibility 检查，UI 显示 `Not eligible`。通过项只有 complete lineage、out-of-sample report、complete provenance；required lenses、minimum coverage、minimum samples、Holm significance、subperiod consistency、cost-aware outcome、complete source provenance、deterministic execution、ABI v2 expressible、buildable 均失败。
5. 确认没有当前 Component Eligible Decision；系统没有自动 Promotion。

### 5.7 Strategy、Paper、Bot、Feedback

1. 进入 `/strategies`：页面明确显示需要一个已接受的 Factor Component output 和一个已接受的 Model qualification；Factor output selector 没有可接受项，Strategy Candidate 数量为 0，Qualification controls disabled。
2. 进入 `/paper-trading`：显示没有保留的 OKX Demo Paper Account evidence；页面不会在进入时联系 Provider。
3. 进入 `/bots`：Strategy Qualification 为 `Select an eligible Qualification`，部署按钮 disabled，显示 `No Bots have been deployed.`。
4. 进入 `/paper-feedback`：显示没有拥有当前 Runtime Attempt、可用于反馈的 Bot，尚未生成新的 feedback report。
5. 该结果是安全门禁的预期终点：没有 Raw Candidate 越权进入 Bot，没有使用无关的历史 Strategy，也没有向 Provider 提交订单。

## 6. zh-CN Computer Use 逐步复跑

1. 进入 Settings → General，将 Interface language 改为 `简体中文`，确认语言值已持久化。
2. 返回 `/`，确认 `系统仪表盘`、`本地系统 关键`、`执行适配器 健康`、`模拟账户 未知`、`没有保留 Bot Runtime 记录。`、`没有保留 OKX Demo 模拟账户。` 和 `研究证据` 均可见。
3. 进入 `运行仪表盘`，确认 `关键运行条件`、`freeze_all_requested`、Host evidence 和 `前端投影只读` 可见。
4. 进入 `特征工程`，确认 `Research Evidence Context 已准备`、Revision 3、crypto/okx、正确 Snapshot、窗口、Feature Dataset、Feature Plan 和 PIT Universe 可见。
5. 进入 `因子研究`：
   - Candidate tab 显示 `EMA 5/10 Crossover Buy Signal`、Candidate hash 前缀 `ca1c448...`、绑定 Context Revision 3、历史失败 Attempt 的中文诊断。
   - Dataset tab 显示 `启动 Factor Dataset 物化`、正确 Candidate、`18 / 18`、Dataset `748e016...` 和已完成 Attempt。
   - Evaluation tab 显示正确 Dataset、`buy-signal`、`out-of-sample`、Report `553113...` 和 1/1 completed。
   - Decision tab 显示 `已拒绝`、`buy-signal`、Promotion protocol 前缀、历史 Decision；同时显示没有当前 Component Eligible Decision。
6. 进入 `策略实验室`，确认中文策略候选页面显示需要已接受 Factor Component 和 Model qualification、当前没有 Strategy Candidate、Qualification controls disabled。
7. 进入 `模拟交易`、`模拟反馈`、`Bot 管理`，分别确认没有账户证据、没有可反馈 Bot、部署按钮 disabled 且尚未部署 Bot。
8. zh-CN 全流程没有发现英文缺失导致的空白、错误 Healthy、可越权的操作按钮或崩溃。

## 7. 真实数据库记录审计

数据库：`/Users/tony/Library/Application Support/bid.adaq.desktop/adaq.db`。以下是本次运行后实时查询的结果：

| 记录 | 数量/结果 |
|---|---:|
| `strategy_candidates` | 0 |
| `strategy_candidate_revisions` | 0 |
| `strategy_qualifications` | 0 |
| `bots` | 0 |
| `paper_accounts` | 0 |
| Candidate build attempts | 17（含历史失败与成功记录） |
| Factor materialization attempts | 17（最终相关 Attempt 18/18） |
| Factor evaluation reports | 5（最终相关 Report `553113...`） |
| Factor promotion decisions | 2（最终相关 Decision 为 Rejected） |
| Candidate predecessor rows | 2（最终 `ca1c...` 已绑定 Revision 3） |

未创建 Bot、Paper Account、Strategy Candidate 或 Strategy Qualification，所以没有可供分析的 runtime attempt、order、fill、position 或 Bot PnL 结果。详见 [V1模拟盘Bot分析报告.md](V1模拟盘Bot分析报告.md)。

## 8. 用户手工复核清单

请在同一个 debug App 中按以下顺序复核：

1. `/`：System Dashboard / 状态卡 / empty 状态 / owning links / read-only 边界。
2. `/operations`：Critical `freeze_all_requested`、Health、Alerts、Evidence 和 recovery link。
3. `/features`：Revision 3、Snapshot、正确时间范围、Feature Dataset 与 Feature Plan。
4. `/factors`：Candidate → Dataset 18/18 → Evaluation 1/1 → Decision Rejected / Not eligible。
5. `/strategies`：无 accepted Factor Component，因此 Strategy Candidate 与 Qualification 操作保持禁用。
6. `/paper-trading`、`/bots`、`/paper-feedback`：账户/Bot/反馈的空状态和禁用部署路径。
7. Settings → General：在 en-US 与 zh-CN 之间切换，重复首页与上述核心路径。

本次没有修改全局配置、没有录入或变更 OKX 凭据、没有向交易 Provider 下单，也没有删除历史研究记录。
