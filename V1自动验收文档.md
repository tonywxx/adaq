# ADAQ V1 自动验收摘要（当前权威）

> 本文件是 V1 工程自动验收的当前权威摘要。带日期的长篇过程记录（2026-09-04 基线验收与 2026-09-09 多轮验证）已移出主导航，归档至 [docs/research/v1-acceptance-process-log.md](docs/research/v1-acceptance-process-log.md)，仅作历史证据，不覆盖本摘要。

## 验收结论

**AdaQ V1 工程自动验收已成功** —— 代码、构建、测试、组件、研究链路、Desktop、Paper Reconcile 与 Bot 安全生命周期已通过验收。

- 最新自动化验收：[V1 Acceptance run 34547508572](https://github.com/tonywxx/adaq/actions/runs/34547508572) 在 macOS ARM64 与 Windows x86_64 上全绿，运行于提交 `108949b`（当前 `origin/main`）。
- 发布：[release v0.9.8](https://github.com/tonywxx/adaq/releases/tag/v0.9.8)，绑定提交 `19ea5f0`。
- 门禁：Rust `cargo fmt/test/check --workspace`、前端 Jest、`pnpm run build`、`pnpm run lint`、`format:check`、`git diff --check`、Python tutorial parity 与 Release 打包均通过。

## 通过范围

| 范围 | 已通过的证据 |
| --- | --- |
| 代码与构建 | 全 Rust 工作区测试与检查（601 passed / 12 ignored / 42 suites 记录于末轮复验）、严格 TypeScript + Vite 构建、Jest、Lint、Format 全部通过 |
| 组件 | `Factor EMA 5/10 Crossover 0.1.1` 包构建/验证与精确 hash；Qlib Ridge Model 包 qualified；示例组件 build + verify、Indicator/Feature/Factor 生成物检查通过 |
| 研究链路 | Data Foundation、Snapshot/Universe、Feature Dataset、Factor Decision（Component Eligible 13/13）、Model、Strategy Qualification、组合 Backtest 与 Validation 的持久化证据完整、可追溯，负结果如实保留 |
| Desktop | 可交互 en-US/zh-CN 界面：System Dashboard、Data Foundation、Feature、Factor、Paper Trading、Bot、Operations；双语切换不改变 Host 证据或 Bot 状态 |
| Paper Reconcile | 通过 `.env` 专用本地凭据路径（`local-env-credentials` Debug 构建，不读取 Keychain，凭据不入库/日志/文档）完成真实 OKX Demo Reconcile，Provider operation `reconcile-1788997971055`，账户 `723843360829982304` |
| Bot 安全生命周期 | 真实完成 Deploy → Start → `account-reconciled` → `warmup-started` → `worker-heartbeat`；输入不完整时 Host Decision Batch 安全返回 `no-target`（`decision-batch-unavailable`，`skipDecision`）；`Stop · Keep position` 安全停止并释放账户 lease |

## 明确边界：验收成功 ≠ 盈利 ≠ Live 授权

- 该结论**不代表策略盈利**。组合 Backtest `portfolio-7fc86a3e…` 总收益率约 -3.0104%，最大回撤约 -5.5690%；Validation 样本外平均收益约 -0.5521%、样本外平均 Sharpe 为 0。负结果被如实保留，不粉饰为正向。
- 该结论**不授权 Live Trading**。V1 严格限于 OKX Demo 与模拟执行；Gate 12 把 autonomous execution 排除在 V1 范围外，不存在真实资金路径。
- 当前 Demo Bot **无成交（0 fills）**、无 realized feedback 样本。Host 在缺少完整 Feature Dataset cross-section 时安全返回 `no-target`，因此不存在可归因的 Bot PnL、胜率、滑点或方向性反馈；Paper Feedback 的 Factor/Model/Strategy/Execution 报告不能据此给出方向性结论。
- 下一步剩余的唯一产品证据链是：为冻结上下文准备完整 current cross-section → 重新 Qualification/Deploy → Host Decision → 若风险门禁允许则真实 Demo Order/Fill → Stop/Reconcile → Paper Feedback reports。

Bot 的运行、账户、回测与风险解读见 [V1模拟盘Bot分析报告.md](V1模拟盘Bot分析报告.md)（0 fills、非收益证明）。

## 本地凭据规则（延续有效）

所有本地 OKX Demo 校对只允许读取仓库根目录 `.env`（`local-env-credentials` Debug 专用构建；非 Debug 构建编译失败），缺失变量立即 fail closed，不回退 Keychain。正式应用与正式用户操作继续使用默认 `KeyringSecretStore` 构建。凭据值不得进入公共仓库、文档、日志、命令输出、前端状态、SQLite 或 GitHub artifact。
