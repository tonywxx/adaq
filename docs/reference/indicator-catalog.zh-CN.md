# ADAQ 指标目录

生成自 `catalog.json`；请勿编辑。

160 个指标 / 179 个输出。每个条目的回溯长度均可从主机目录获取；`unstablePeriod` 是官方的 TA-Lib 标志，并非收敛声明。

## `accbands` (ACCBANDS)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `20`；范围 `2`–`100000`.

### 输出

- `upper-band` — 双精度数组。
- `middle-band` — 双精度数组。
- `lower-band` — 双精度数组。

## `acos` (ACOS)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ad` (AD)

分组：成交量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。
- `volume` — 成交量；显式成交量。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `add` (ADD)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `real-1` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `adosc` (ADOSC)

分组：成交量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。
- `volume` — 成交量；显式成交量。

### 参数

- `fast-period` — 整数；默认 `3`；范围 `2`–`100000`.
- `slow-period` — 整数；默认 `10`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `adx` (ADX)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `adxr` (ADXR)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `apo` (APO)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `fast-period` — 整数；默认 `12`；范围 `2`–`100000`.
- `slow-period` — 整数；默认 `26`；范围 `2`–`100000`.
- `ma-type` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `value` — 双精度数组。

## `aroon` (AROON)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `aroon-down` — 双精度数组。
- `aroon-up` — 双精度数组。

## `aroonosc` (AROONOSC)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `asin` (ASIN)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `atan` (ATAN)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `atr` (ATR)

分组：波动率指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `avgdev` (AVGDEV)

分组：价格变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `avgprice` (AVGPRICE)

分组：价格变换。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `bbands` (BBANDS)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `5`；范围 `2`–`100000`.
- `deviations-up` — 双精度；default `2.000000e+0`；范围 `-3.000000e+37`–`3.000000e+37`.
- `deviations-down` — 双精度；default `2.000000e+0`；范围 `-3.000000e+37`–`3.000000e+37`.
- `ma-type` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `upper-band` — 双精度数组。
- `middle-band` — 双精度数组。
- `lower-band` — 双精度数组。

## `beta` (BETA)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `real-1` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `5`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `bop` (BOP)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `cci` (CCI)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `cdl2crows` (CDL2CROWS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdl3blackcrows` (CDL3BLACKCROWS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdl3inside` (CDL3INSIDE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdl3linestrike` (CDL3LINESTRIKE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdl3outside` (CDL3OUTSIDE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdl3starsinsouth` (CDL3STARSINSOUTH)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdl3whitesoldiers` (CDL3WHITESOLDIERS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlabandonedbaby` (CDLABANDONEDBABY)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `3.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdladvanceblock` (CDLADVANCEBLOCK)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlbelthold` (CDLBELTHOLD)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlbreakaway` (CDLBREAKAWAY)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlclosingmarubozu` (CDLCLOSINGMARUBOZU)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlconcealbabyswall` (CDLCONCEALBABYSWALL)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlcounterattack` (CDLCOUNTERATTACK)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdldarkcloudcover` (CDLDARKCLOUDCOVER)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `5.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdldoji` (CDLDOJI)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdldojistar` (CDLDOJISTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdldragonflydoji` (CDLDRAGONFLYDOJI)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlengulfing` (CDLENGULFING)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdleveningdojistar` (CDLEVENINGDOJISTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `3.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdleveningstar` (CDLEVENINGSTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `3.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdlgapsidesidewhite` (CDLGAPSIDESIDEWHITE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlgravestonedoji` (CDLGRAVESTONEDOJI)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlhammer` (CDLHAMMER)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlhangingman` (CDLHANGINGMAN)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlharami` (CDLHARAMI)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlharamicross` (CDLHARAMICROSS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlhighwave` (CDLHIGHWAVE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlhikkake` (CDLHIKKAKE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlhikkakemod` (CDLHIKKAKEMOD)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlhomingpigeon` (CDLHOMINGPIGEON)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlidentical3crows` (CDLIDENTICAL3CROWS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlinneck` (CDLINNECK)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlinvertedhammer` (CDLINVERTEDHAMMER)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlkicking` (CDLKICKING)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlkickingbylength` (CDLKICKINGBYLENGTH)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlladderbottom` (CDLLADDERBOTTOM)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdllongleggeddoji` (CDLLONGLEGGEDDOJI)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdllongline` (CDLLONGLINE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlmarubozu` (CDLMARUBOZU)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlmatchinglow` (CDLMATCHINGLOW)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlmathold` (CDLMATHOLD)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `5.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdlmorningdojistar` (CDLMORNINGDOJISTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `3.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdlmorningstar` (CDLMORNINGSTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `penetration` — 双精度；default `3.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 整型数组。

## `cdlonneck` (CDLONNECK)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlpiercing` (CDLPIERCING)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlrickshawman` (CDLRICKSHAWMAN)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlrisefall3methods` (CDLRISEFALL3METHODS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlseparatinglines` (CDLSEPARATINGLINES)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlshootingstar` (CDLSHOOTINGSTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlshortline` (CDLSHORTLINE)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlspinningtop` (CDLSPINNINGTOP)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlstalledpattern` (CDLSTALLEDPATTERN)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlsticksandwich` (CDLSTICKSANDWICH)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdltakuri` (CDLTAKURI)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdltasukigap` (CDLTASUKIGAP)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlthrusting` (CDLTHRUSTING)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdltristar` (CDLTRISTAR)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlunique3river` (CDLUNIQUE3RIVER)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlupsidegap2crows` (CDLUPSIDEGAP2CROWS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `cdlxsidegap3methods` (CDLXSIDEGAP3METHODS)

分组：形态识别。官方不稳定期：`false`（否）。

### 输入

- `open` — 开盘价；固定市场。
- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `ceil` (CEIL)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `cmo` (CMO)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `correl` (CORREL)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `real-1` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `cos` (COS)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `cosh` (COSH)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `dema` (DEMA)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `div` (DIV)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `real-1` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `dx` (DX)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `ema` (EMA)

分组：重叠研究。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `exp` (EXP)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `floor` (FLOOR)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ht-dcperiod` (HT_DCPERIOD)

分组：周期指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ht-dcphase` (HT_DCPHASE)

分组：周期指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ht-phasor` (HT_PHASOR)

分组：周期指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `in-phase` — 双精度数组。
- `quadrature` — 双精度数组。

## `ht-sine` (HT_SINE)

分组：周期指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `sine` — 双精度数组。
- `lead-sine` — 双精度数组。

## `ht-trendline` (HT_TRENDLINE)

分组：重叠研究。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ht-trendmode` (HT_TRENDMODE)

分组：周期指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 整型数组。

## `imi` (IMI)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `open` — 开盘价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `kama` (KAMA)

分组：重叠研究。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `linearreg` (LINEARREG)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `linearreg-angle` (LINEARREG_ANGLE)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `linearreg-intercept` (LINEARREG_INTERCEPT)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `linearreg-slope` (LINEARREG_SLOPE)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `ln` (LN)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `log10` (LOG10)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ma` (MA)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.
- `ma-type` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `value` — 双精度数组。

## `macd` (MACD)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `fast-period` — 整数；默认 `12`；范围 `2`–`100000`.
- `slow-period` — 整数；默认 `26`；范围 `2`–`100000`.
- `signal-period` — 整数；默认 `9`；范围 `1`–`100000`.

### 输出

- `macd` — 双精度数组。
- `signal` — 双精度数组。
- `histogram` — 双精度数组。

## `macdext` (MACDEXT)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `fast-period` — 整数；默认 `12`；范围 `2`–`100000`.
- `fast-ma` — 均线类型；默认 `0`；范围 ``–``.
- `slow-period` — 整数；默认 `26`；范围 `2`–`100000`.
- `slow-ma` — 均线类型；默认 `0`；范围 ``–``.
- `signal-period` — 整数；默认 `9`；范围 `1`–`100000`.
- `signal-ma` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `macd` — 双精度数组。
- `signal` — 双精度数组。
- `histogram` — 双精度数组。

## `macdfix` (MACDFIX)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `signal-period` — 整数；默认 `9`；范围 `1`–`100000`.

### 输出

- `macd` — 双精度数组。
- `signal` — 双精度数组。
- `histogram` — 双精度数组。

## `mama` (MAMA)

分组：重叠研究。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `fast-limit` — 双精度；default `5.000000e-1`；范围 `1.000000e-2`–`9.900000e-1`.
- `slow-limit` — 双精度；default `5.000000e-2`；范围 `1.000000e-2`–`9.900000e-1`.

### 输出

- `mama` — 双精度数组。
- `fama` — 双精度数组。

## `max` (MAX)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `maxindex` (MAXINDEX)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `value` — 整型数组。

## `medprice` (MEDPRICE)

分组：价格变换。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `mfi` (MFI)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。
- `volume` — 成交量；显式成交量。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `midpoint` (MIDPOINT)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `midprice` (MIDPRICE)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `min` (MIN)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `minindex` (MININDEX)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `value` — 整型数组。

## `minmax` (MINMAX)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `min` — 双精度数组。
- `max` — 双精度数组。

## `minmaxindex` (MINMAXINDEX)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `min-idx` — 整型数组。
- `max-idx` — 整型数组。

## `minus-di` (MINUS_DI)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `minus-dm` (MINUS_DM)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `mom` (MOM)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `10`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `mult` (MULT)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `real-1` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `natr` (NATR)

分组：波动率指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `obv` (OBV)

分组：成交量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `volume` — 成交量；显式成交量。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `plus-di` (PLUS_DI)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `plus-dm` (PLUS_DM)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `ppo` (PPO)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `fast-period` — 整数；默认 `12`；范围 `2`–`100000`.
- `slow-period` — 整数；默认 `26`；范围 `2`–`100000`.
- `ma-type` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `value` — 双精度数组。

## `roc` (ROC)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `10`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `rocp` (ROCP)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `10`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `rocr` (ROCR)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `10`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `rocr100` (ROCR100)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `10`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `rsi` (RSI)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `sar` (SAR)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `acceleration-factor` — 双精度；default `2.000000e-2`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-maximum` — 双精度；default `2.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 双精度数组。

## `sarext` (SAREXT)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。

### 参数

- `start-value` — 双精度；default `0.000000e+0`；范围 `-3.000000e+37`–`3.000000e+37`.
- `offset-on-reverse` — 双精度；default `0.000000e+0`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-init-long` — 双精度；default `2.000000e-2`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-long` — 双精度；default `2.000000e-2`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-max-long` — 双精度；default `2.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-init-short` — 双精度；default `2.000000e-2`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-short` — 双精度；default `2.000000e-2`；范围 `0.000000e+0`–`3.000000e+37`.
- `af-max-short` — 双精度；default `2.000000e-1`；范围 `0.000000e+0`–`3.000000e+37`.

### 输出

- `value` — 双精度数组。

## `sin` (SIN)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `sinh` (SINH)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `sma` (SMA)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `sqrt` (SQRT)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `stddev` (STDDEV)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `5`；范围 `2`–`100000`.
- `deviations` — 双精度；default `1.000000e+0`；范围 `-3.000000e+37`–`3.000000e+37`.

### 输出

- `value` — 双精度数组。

## `stoch` (STOCH)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `fast-k-period` — 整数；默认 `5`；范围 `1`–`100000`.
- `slow-k-period` — 整数；默认 `3`；范围 `1`–`100000`.
- `slow-k-ma` — 均线类型；默认 `0`；范围 ``–``.
- `slow-d-period` — 整数；默认 `3`；范围 `1`–`100000`.
- `slow-d-ma` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `slow-k` — 双精度数组。
- `slow-d` — 双精度数组。

## `stochf` (STOCHF)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `fast-k-period` — 整数；默认 `5`；范围 `1`–`100000`.
- `fast-d-period` — 整数；默认 `3`；范围 `1`–`100000`.
- `fast-d-ma` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `fast-k` — 双精度数组。
- `fast-d` — 双精度数组。

## `stochrsi` (STOCHRSI)

分组：动量指标。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.
- `fast-k-period` — 整数；默认 `5`；范围 `1`–`100000`.
- `fast-d-period` — 整数；默认 `3`；范围 `1`–`100000`.
- `fast-d-ma` — 均线类型；默认 `0`；范围 ``–``.

### 输出

- `fast-k` — 双精度数组。
- `fast-d` — 双精度数组。

## `sub` (SUB)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。
- `real-1` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `sum` (SUM)

分组：数学运算符。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `t3` (T3)

分组：重叠研究。官方不稳定期：`true`（是）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `5`；范围 `1`–`100000`.
- `volume-factor` — 双精度；default `7.000000e-1`；范围 `0.000000e+0`–`1.000000e+0`.

### 输出

- `value` — 双精度数组。

## `tan` (TAN)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `tanh` (TANH)

分组：数学变换。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `tema` (TEMA)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `trange` (TRANGE)

分组：波动率指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `trima` (TRIMA)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `trix` (TRIX)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `tsf` (TSF)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `typprice` (TYPPRICE)

分组：价格变换。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `ultosc` (ULTOSC)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `first-period` — 整数；默认 `7`；范围 `1`–`100000`.
- `second-period` — 整数；默认 `14`；范围 `1`–`100000`.
- `third-period` — 整数；默认 `28`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。

## `var` (VAR)

分组：统计函数。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `5`；范围 `1`–`100000`.
- `deviations` — 双精度；default `1.000000e+0`；范围 `-3.000000e+37`–`3.000000e+37`.

### 输出

- `value` — 双精度数组。

## `wclprice` (WCLPRICE)

分组：价格变换。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- 无。

### 输出

- `value` — 双精度数组。

## `willr` (WILLR)

分组：动量指标。官方不稳定期：`false`（否）。

### 输入

- `high` — 最高价；固定市场。
- `low` — 最低价；固定市场。
- `close` — 收盘价；固定市场。

### 参数

- `time-period` — 整数；默认 `14`；范围 `2`–`100000`.

### 输出

- `value` — 双精度数组。

## `wma` (WMA)

分组：重叠研究。官方不稳定期：`false`（否）。

### 输入

- `real-0` — 双精度数组；通用-OHCL-实值。

### 参数

- `time-period` — 整数；默认 `30`；范围 `1`–`100000`.

### 输出

- `value` — 双精度数组。
