# Close Score Model

Minimal deterministic Model Component teaching fixture. It forwards the `close` Feature Slot as a one-Bar `next-close-score`, preserving every input row identity and order.

From this directory, run `../../../src-tauri/target/debug/adaq-component build`, then verify the generated package with `adaq-component verify dist/model-close-score-0.1.0.adaq`.
