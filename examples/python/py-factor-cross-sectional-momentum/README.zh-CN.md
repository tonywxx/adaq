# Python 横截面动量因子

这是用于 ADAQ M12 离线教程的 Apache-2.0、可查看源代码的**合成演示**项目。
它接收 Host 提供的收盘价证据，按 `收盘价 → 后向简单收益 → 横截面百分位 →
动量分数` 构建因子，由 Host 管理 `lookback={5,20,60}` 网格，默认值为 20。

项目不包含市场数据、凭据、Runtime、Environment 或结果。ADAQ 会先静态验证源代码，
只有明确受信任的不可变 Revision 才能执行。
