[package]
name = "adaq"
version = "0.8.3"
description = "AI Quant Trading"
authors = ["TONy.W"]
edition = "2024"

[workspace]
members = [
    "crates/ada-data-core",
    "crates/ada-backtest-core",
    "crates/adaq-component-sdk",
    "crates/adaq-component-tooling",
    "crates/adaq-indicator-engine",
]
resolver = "2"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "adaq_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
ada-data-core = { path = "crates/ada-data-core" }
ada-backtest-core = { path = "crates/ada-backtest-core" }
adaq-component-sdk = { path = "crates/adaq-component-sdk", features = ["host"] }
adaq-component-tooling = { path = "crates/adaq-component-tooling" }
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-opener = "2"
tauri-plugin-process = "2"
tauri-plugin-updater = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-log = "2"
tauri-plugin-http = "2"
rusqlite = { version = "0.37", features = ["bundled"] }
rust_decimal = "1"
sha2 = "0.11"

[dev-dependencies]
zip = { version = "7", default-features = false, features = ["deflate"] }
