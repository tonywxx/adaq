[package]
name = "adaq-bot-runtime"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"
description = "Host-owned, fail-closed Trading Bot runtime contracts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.11"
